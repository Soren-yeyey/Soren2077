# 2026-09-21（Day 6）

## 今日清单（Day 6｜第 1 周）
- 今天要掌握：你给自己项目加了哪条新规则？它防的是哪种情况？
- 板块顺序：① 检查 Day 1 的规则文件 ② 追加自己的规则 ③ 让 AI 复述确认
- 主任务：检查并完善 AGENTS.md
- 今日不做：重写整个规则文件（**只允许追加**）；写代码
- 完成标准：AGENTS.md 在项目里；Day 1 规则仍在；重点核对「当天只做当天任务，学员没说『进入下一板块』就不越界」这条还在；追加 ≥1 条自己的规则；AI 能复述出规则内容
- 交图：打开 AGENTS.md —— 图里要有**新追加的规则** + 「当天只做当天任务」那条**仍在**
- 收尾：列改动清单（含归属天数）→ 提交推送（`Day 6｜一句话`，说明两行：改了什么 / 加了什么）→ 停下等确认
- 余力加练：把其中一条规则改写成队友也能用的检查项
- 卡住降级：规则文件丢失就重新粘贴 Day 1 模板，再追加自己的规则

## 开工只读探测
- HEAD = `f8224b0`（Day 5 提交），工作区仅 2 个记忆文件改动（Day 5 遗留：`2026-09-20.md`、`MEMORY.md`）
- `git diff --stat HEAD -- AGENTS.md` **无输出** → AGENTS.md 与 HEAD 逐字节一致，Day 1 原版未被后来改动

## 板块① 完成（检查 Day 1 规则文件）
- 文件在：`D:\workbuddy\AGENTS.md`，HEAD 版本 = 52 行 / 3996 字节
- 七节齐全：一 任务边界 / 二 交互式推进 / 三 人机分工 / 四 讲清楚环节 / 五 Git 与提交 / 六 当日收尾 / 七 出问题的时候
- **「当天只做当天任务」位置：第 5–7 行**（第一节「任务边界」第 1–2 点）

## 板块② 完成（追加个人规则）
- 用户回复「写」→ 理解为**三条全选**
- 追加方式：`Edit` 在文件**末尾**追加，**原有七节零改动**（严守清单「只允许追加」）
- 新增三节共 9 条：
  - **八 文件与素材**（3 条）：图片放 `images/`；文件名英文小写 + 连字符；单张 ≤1.0 MB / 总量 ≤5 MB，先压缩再入库；新增文件前先报目录与文件名
  - **九 文案与命名**（3 条）：界面与文案全中文、不夹英文不用梗；禁「赛博朋克 2077」「Night City」等商标，风格词「赛博朋克」可用；文案定稿前先给原文
  - **十 怎么算交付**（3 条）：交付须给「① 打开什么 ② 点什么/看哪里 ③ 应看到什么（含出错表现）」三步；禁止只说"已经改好了"；验证失败先重试一次，再失败停下报告
- 每条规则括号内附了**理由**（防什么）：防 Windows 不区分大小写 / Linux 区分大小写导致的**线上 404**；防大图**污染 Git 历史（不可逆，删文件也清不掉）**
- 验证硬证据（可复现）：
  - `git diff --numstat -- AGENTS.md` = **18 新增 / 0 删除**
  - 52 行 / 3996 字节 → **70 行 / 5518 字节**（差 18 行 / 1522 字节，与新增量吻合）
  - `git diff -U0 -- AGENTS.md | grep '^-'`（排除 `---` 头）**无输出** = 原有内容一行未删
- 顺带修了一处措辞：「名字对不上」→「大小写对不上」，更准确

## 板块③ 完成（复述确认）
- 逐节复述全部十节规则（Day 1 七节 + 新增三节），并特别点出三条最易被我越界的：「进入下一板块」**只解锁当前这一步**、提交前**必须列清单等你确认**、同一操作**反复失败要停手报告**
- 用户回「进入下一板块」= 复述通过、无异议

## 收尾（2026-09-21，用户回「提交」）
- 提交前核对：本地 HEAD = 远程 main = `f8224b0`（起点同步）；`.env` 被忽略（`git check-ignore -v` 命中 `.gitignore:1:.env`）✓
- 暂存清单 4 项：`AGENTS.md`(Day 6) / `.workbuddy/memory/2026-09-21.md`(Day 6 新建) / `.workbuddy/memory/2026-09-20.md`(Day 5 收尾) / `.workbuddy/memory/MEMORY.md`(Day 5+6)
- commit 标题：`Day 6｜完善项目规则文件 AGENTS.md`；说明两行：改了什么（末尾追加三节、原有七节零改动，18 增 / 0 删）/ 加了什么（八 文件与素材、九 文案与命名、十 怎么算交付，共 9 条）

## 待办
- 交图提醒（已给用户）：新规则在第 54–70 行、「当天只做当天任务」在第 5–7 行，隔约 55 行，**截图需拉高窗口或调小字号才能同屏**
- Day 7：素材入库前必须压到 ≤1.0 MB/张，且**先压缩再 commit**
