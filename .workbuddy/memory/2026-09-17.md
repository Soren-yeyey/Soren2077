# 2026-09-17

## Day 2 计划与拍板

- 只读探测结果：`gh` CLI **未安装**（远程仓库无法由 AI 直接创建，必须用户在 GitHub 网页点）；`init.defaultBranch` 未设置；`credential.helper` 未设置（首次 push 会弹浏览器授权）；工作区当时只有 `AGENTS.md` + `.workbuddy\`。
- 拍板结果：
  - 仓库名 = **Soren2077**（用户先选含中文的「Soren的不夜城」，经提醒 URL 会被百分号编码后改为纯 ASCII 名）
  - 可见性 = **Public 公开**
  - 主分支 = **main**
  - 选项 4 未答 → 按默认 A：创建空 `.env` 实测忽略生效
  - 选项 5 未答 → 按默认 A：`.gitignore` 只写 `.env`（严格按清单，不顺手加 node_modules 等）
- 计划内已声明：本地操作（建文件 / init / add / commit / push）由 AI 代办；GitHub 网页新建仓库 + 首次浏览器授权必须用户亲自做。
- 已向用户指出规则冲突：清单收尾指令"提交并推送后再确认" vs AGENTS.md 第五节"提交前先列清单等确认"，**默认按 AGENTS.md 执行**（先列清单 → 确认 → 提交推送），等用户表态。

- 板块① 学习问题，用户答："担心没有公开，或者没有成功存进去"。→ 板块② 需针对这两点给出可亲眼确认的验证手段（仓库页 Public 标签 / 匿名访问；`git ls-remote origin refs/heads/main` 的 hash 与本地 `git rev-parse HEAD` 比对）。

## Day 2 执行结果

- 板块② 全部完成。首次提交 hash = **`d4d5f208e45401968dedb76e0d59999748dee55e`**（commit 标题 `Day 2｜建仓库并首次提交占位页`），6 个文件 / 133 行新增。
- 推送成功：`git ls-remote origin refs/heads/main` 返回的 hash 与本地 `git rev-parse HEAD` **完全一致**。
- 用户侧完成的两次人工操作：① GitHub 网页新建空仓库；② 浏览器完成 Git Credential Manager 的 sudo 模式授权（页面提示 "Authentication Succeeded"）。
- 公开性验证：匿名 `curl` 仓库主页 → HTTP 200；匿名调 GitHub API `contents/` → 返回 `.gitignore`、`.workbuddy`、`AGENTS.md`、`index.html`（**无 `.env`**，忽略规则生效）。
- 用户选 C：`.workbuddy/` 随仓库公开，其中 `MEMORY.md` 第 24 行的真实 QQ 邮箱已公开（已明确告知，用户确认）。

## 环境异常（待跟进）

- **`refs/remotes/origin/main` 写不进去**：`git fetch origin` 报告 `* [new branch] main -> origin/main`、退出码 0，但 `.git/refs/remotes/` 始终为空；手动 `git update-ref refs/remotes/origin/main <hash>` 同样退出码 0 却不落盘。
- 已排除文件系统原因：手动 `mkdir -p` + `echo > .git/refs/remotes/origin/probe.txt` 能正常持久化（探针文件已清理）。
- 影响面：仅本地显示层——`git status -sb` 显示 `## main...origin/main [gone]`，`git ls-tree origin/main` 报 `Not a valid object name`。**push / fetch / commit 均正常**，远程事实改用 `git ls-remote origin` 查询即可。
- 按 AGENTS.md 七.3 已停止重试，未继续深挖。Day 3 开工时可再查一次。

## 待办

- 用户尚未回答"是否把清单驱动+单步推进固化成 skill"（Day 1 遗留，用户未表态）。
- Day 1 余力加练（前端 / 后端 / 数据库）未做，用户可选。
- Day 2 余力加练（本地回退到上一版本）未做，用户可选。
- Day 3 开工先复查 `refs/remotes/origin/*` 写入异常是否仍在。

## 收尾核对（用户 19:05 说「今天做完了」）

- 完成标准 1「GitHub 网页能看到仓库和 index.html」→ 已完成。证据：用户在**未登录浏览窗口**打开 `https://github.com/Soren-yeyey/Soren2077` 截图，含 `Public` 徽章、`index.html`、commit `d4d5f20 Day 2｜建仓库并首次提交占位页`。
- 完成标准 2「忽略文件生效，.env 不被上传」→ 已完成。证据：网页文件列表无 `.env`；匿名 GitHub API `contents/` 只返回 `.gitignore`/`.workbuddy`/`AGENTS.md`/`index.html`；`git status --ignored` 显示 `!! .env`。
- 截图要求（仓库名 + index.html + commit 以「Day 2｜」开头）→ 达标，已交。
- 今日不做项：网站功能开发（未做，index.html 仅占位）、背 Git 命令（未要求，仅解释名词）→ 均遵守。
- 余力加练未做（可选，不算未达标）。
- Day 2 全部完成，无未完成项。

