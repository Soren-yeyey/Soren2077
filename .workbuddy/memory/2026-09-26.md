# 2026-09-26（用户口径 Day 11 · 续）

## 任务：完成一个有反馈的交互 —— 武器箱 → 获得「不夜恶魔」手枪 → 试射

用户在 Day 11 清单里把交互选题改为：**完成简单任务获得赛博风格手枪，可以开枪射击**（新构思第一次落进项目）。选了「甲」= 最薄完整闭环，手枪命名**「不夜恶魔」**（完全虚构，不碰商标）。

## 交付内容（全部完成，未提交）

### 素材（2 张，走参考图风格重制）
- 用户对第一版不满意（太素），发了参考图 → 用图生图按参考图风格重出，用户确认「可以了」
- `images/gun-cyber-pistol.webp`（520×342 / 111 KB，粉紫霓虹电路纹手枪）
- `images/prop-weapon-case.webp`（320×231 / 40 KB，开盖武器箱、品红能量核心）
- 处理流程：ImageGen 出图（黑底+描边光）→ 右下「AI生成」水印填色去除 → 黑底按亮度抠透明 → 裁主体 → webp。总素材 1.13 MB，仍在 5 MB 红线内

### `index.html` 改动（+293 行）
- CSS 第 ⑩ 节：武器箱贴图/光圈、`#gunBadge` 枪徽标（右下 64×64 常驻）、`.bullet-hole` 弹孔（黑心+品红冲击晕）、`.impact-ring` 冲击环、`.shake` 物件受击抖动、toast 提示条
- 第 ⑨ 节 `:is()` 统一约束纳入 `#gunBadge`
- HTML：场景内第 4 个交互物 `.hotspot[data-id=case]` + `.prop` 贴图（72%,62%，不遮原三物件）；文字层加 toast + 徽标
- JS：`HOLES_MAX = 8`（场上弹孔上限，最旧让位）；开箱 → 旁白 → 徽标入场 + toast「获得武器：不夜恶魔」；徽标点击切试射（`aria-pressed` 同步）；试射中点地面=开枪（弹孔+冲击环，不走人）、点物件=命中抖动但**不推进剧情**；复位时枪收走、弹孔清光、箱子重新「没开过」

## 验证结论（无头 390×844，全绿）

| 链路 | 结果 |
|---|---|
| 获得链 | 开箱命中 BUTTON → 旁白 2 段 → 徽标 64×64 过 44 线 → toast 文案正确 → 箱子光圈收 |
| 试射链 | 徽标切试射 aria=true → 空地弹孔/冲击环生成 → 打洗衣机抖动且**不误推进剧情** → 收枪成功 |
| 连续压测 | 徽标连切 10 次=偶数次正确复位；试射中乱点 20 下（地面+物件混点）状态一致、进度仍 0/3、光圈状态正确；已读箱子重复点 3 次不重复给枪；收枪后走动恢复（`left` 50%→14.87% 同步写入）|
| 报错 | `errors` / `console` 零输出 |
| 弹孔 | 实测压测后 8 枚 = 代码 `HOLES_MAX = 8`，吻合 |

无头前后对比图：`C:/Users/18730/AppData/Local/Temp/day11-before-shot.png` / `day11-after-shot.png`（前=toast 提示+徽标亮，后=弹孔品红冲击晕，PIL 放大验证过）。

## 踩坑（3 次同一个）

- **agent-browser 标签页跨 Bash 调用被重置**：今天连中 3 次（报 `Cannot read properties of null`）。解法不变：整条链路放进**同一个 Bash 调用**；第一条 eval 先探测关键元素是否存在，失效就重开页面重放前置步骤
- **测走动查错属性**：位移写在 `style.left/top`（CSS transition），不是 `style.translate`；且短距离 0.5s 内走完，晚采样读到空值 → 误判"角色没动"。正确做法：点击后**同步**读 `style.left` + `walking` 类

## 收尾（已完成）

- 提交 `ee16706 Day 11｜武器箱获得「不夜恶魔」手枪并可试射的完整交互`（6 文件 / +358 −4，含昨日遗留的 2026-09-25.md 补记），推送 `a5b9afd..ee16706` **一次过**
- 线上核验：index.html 去 CR md5 本地/线上同为 `40488681…`，两张新素材 HTTP 200
- 待用户截带地址栏的点击前/后图（线上已是新版，可直接截）
- 余力加练（键盘操作+焦点态）未做

## 待办（下一步）

- 无（等用户下一步指令；「不夜恶魔」后续打磨——准星/连发/更多靶子——等用户发 Day 12 清单）

## 第一人称改造（Day 11 第二批，同日晚些）

用户指令："枪要拿在手上，整个游戏给我第一人称的感觉，第三人称没感觉"。拍板：两屏都第一人称、拿枪前可以空手、其余按我给的方案。

### 交付（已完成并验证，未提交）
- **角色退场**：`#player` / `#shadow` / `#playerHome` / `#shadowHome` 一条 `display:none`（走动机制代码保留，写给隐藏元素无害；恢复第三人称删一行即可）
- **世界层**：两屏各包 `.world`（`inset:-10%` = 视觉 1.2 倍），点地面 → `panWorld()` 世界反向滑（`--pan-x/--pan-y` ±8.333 世界-%，`--pan-dur` 按距离算）；雨挂在取景框不挂世界（淋在镜头前）
- **持枪 viewmodel**：`#viewGun` 挂 `#stage` 内（跟相机不跟世界），试射中从画面外升上来（掏枪 `.drawn` + rAF），后坐 `.fire` 挂外层、呼吸晃动挂 img、枪口火光 `#muzzle` 独立元素；pointer-events:none 点击穿透
- **自动取景 `frameTarget()`**：换幕时把下一幕目标拉回画面中间 50% 横带（贩卖机 92%/收音机 5% 在 1.2 倍下必出框，不做这个流程走不通）
- 首页提示文案改「点地面移动」

### 两个真 bug（都抓到根因）
1. **双重放大**：`.world` 盒子已 120%（inset:-10%）又叠 `scale(1.2)` → 1.44 倍，可视范围只剩画面 69%，贩卖机/收音机彻底出框。教训：**盒子和 scale 只能选一个承担放大**
2. **中心变焦的固有代价**：就算只放大 1.2 倍，场景 92%/5% 的物件中心也必然出框（50±50/1.2 → 8.3~91.7）→ 自动取景是正解，不是可选优化

### 验证（全绿）
首页/店内平移方向与幅度（±6.15/±5.25）、光圈位置（34.4%/76.4% 可见，换幕后贩卖机 90.4% 入框）、三幕 → 结束 → 复位（镜头回正、光圈复位、弹孔清光）、持枪链路（掏枪入画 226px/舞台 390、开枪后坐 + 弹孔 1、收枪）、桌面 1440×810（枪 560px 上限、无溢出）、零报错。截图 `Temp/fp-home.png` / `fp-armed2.png` / `fp-fired2.png` / `fp-desktop.png`。
