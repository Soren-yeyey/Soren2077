# 项目长期记忆 — workbuddy

## 项目约定（硬规则）

- 项目根：`D:\workbuddy`，AGENTS.md 保存用户本人的协作规则（2026-09-16 写入）
- 协作模式：**清单驱动 + 单步推进**。用户每天发「今日任务清单」，清单即当天唯一任务范围
- 收到清单后只输出执行计划并停下，等确认；之后**一次只做一个步骤**，做完报告三件事（做了什么 / 改了哪些文件 / 怎么验证）后停下
- 用户必须回复「进入下一板块」才允许继续；这四个字只对当前步骤有效
- 需要人工操作的事（注册、点按钮、装软件、截图）不代做、不假装完成，只给操作指引后停下
- 需要拍板的事（方案/命名/风格）列选项和代价给用户选，不自己决定
- 生成内容（文档/文件/代码）必须等用户明确说"做"再动手
- 验证必须可亲眼确认（页面/文件/命令输出），不接受口头结论
- Git：授权代提交推送，但提交前先列改动文件清单（含属于哪天的任务）等确认；commit 格式 `Day X｜一句话说明`；一天全部做完并核对后才提交，禁止 `git reset --hard` 和强推，撤销用 `git revert`
- 用户说「今天做完了」→ 逐条对照完成标准输出表格（完成标准 / 状态 / 证据），不得模糊处理
- 密钥、`.env`、连接串永不进代码/提交

## 环境事实（2026-09-16 探测）

- 工作区：`D:\workbuddy`（用户拍板不再改名，就是它）
- Git：`2.55.0.windows.4` 已装
- Node：`v22.22.2`、npm `10.9.7` 已装
- GitHub 账号：**已有**，用户名 `Soren-yeyey`
- Git 身份（已配置进 `C:\Users\18730\.gitconfig`）：`user.name=Soren-yeyey`、`user.email=288827322+Soren-yeyey@users.noreply.github.com`（选 noreply，避免真实 QQ 邮箱外泄）
- GitHub 主邮箱为 `1873036550@qq.com`；「保持我的电子邮件地址私密」开关当时为关闭状态
- Day 1 截图要求（文件管理器展示工作区完整路径 + AGENTS.md）已由用户截好满足

## Day 2 目标仓库（用户拍板）

- GitHub 仓库名：`Soren2077`（纯 ASCII，用户先选含中文的「Soren的不夜城」后改为此名）
- 可见性：**Public**
- 主分支：`main`（用户拍板，与 GitHub 默认一致）
- `.gitignore` 只含 `.env`（严格按清单，用户未要求扩展）；另建空 `.env` 用于实测忽略生效
- 本机无 `gh` CLI → 远程仓库只能由用户在网页手动创建；首次 push 需用户浏览器授权一次
- 仓库地址：`https://github.com/Soren-yeyey/Soren2077.git`，远程名 `origin`，主分支 `main`
- 首次提交 hash：`d4d5f208e45401968dedb76e0d59999748dee55e`（Day 2，6 文件 / 133 行）

## Day 3 目标项目（用户拍板，2026-09-18）

- 项目性质：**AIGC 生成场景的赛博朋克风格交互式游戏**（网页，玩家可操控角色）
- MVP 档位（Day 7）：**单场景可玩小样** —— 1 张 AIGC 场景背景 + 角色**点击目的地移动** + 2–3 个物件点击触发固定剧情
- 目标玩家：**公开发布**，任何人打开链接即玩；电脑与手机都要能玩（靠点击操控，一套代码兼顾，不做两套适配）
- 一局时长目标：**5–10 分钟**（依据：Hidden Door 官方「每场 5–8 分钟」）→ **Day 4 已修正为 3–6 分钟**（原值与文案量矛盾），见下方 Day 4 小节
- 素材策略：**固定 3–5 张高质量场景图**（依据：Hidden Door 官方「少量高质量 > 一大堆平庸的受控生成素材」）
- 分工原则：**AIGC 管「长相」，脚本管「逻辑」**（反面教材：AI Dungeon 纯自由生成导致剧情漂移、掉用户）
- 比较对象：Hidden Door / VA-11 HALL-A / AI Dungeon → 成果落在 `research.md`（Day 3 产出，135 行 / 8 节）
- 文档落点：`research.md` 放仓库根目录（用户拍板，不建 docs/）
- **命名红线**：「赛博朋克 2077」「Night City」是 CD Projekt Red 商标，标题与界面禁用；「cyberpunk / 赛博朋克」作为风格词可用；仓库名 `Soren2077` 无商标问题
- 「本期不做」共 13 条：不做 3D / 多地图 / 战斗 / 联机 / 实时自由生成 / 无限素材 / 剧情分支树 / 键盘操控 / 账号登录 / 存档 / 排行榜成就分享 / 声音；标题界面不含商标
- 用户拍板：**不做声音**（音效与背景乐都不做），氛围靠美术与文本补

## Day 4 作品命名与玩法定稿（用户拍板，2026-09-19）

- **作品名：`夜班 SOLITUDE`**（仓库名仍是 `Soren2077`，两者不同名属正常，无需统一）；已确认无商标冲突
- 玩家能做什么：**场景内移动 + 点击 2–3 个物件触发固定剧情**（保留"操控角色"，不减）
- 有轻目标：**点完 3 个物件 → 触发结束画面**，让玩家知道"玩完了"
- 开场引导：**一句文字提示**（不做三步高亮引导）
- 卡住降级条款（清单原文含"首页/详情/写入功能"，与游戏项目不匹配）：按**精神**理解 —— 真卡住时只写核心玩法 PRD，其余列后续
- `index.html` 的 `<title>` 与 `<h1>` 已按用户拍板统一为 `夜班 SOLITUDE`（288 → 300 字节）
- PRD 落点：`PRD.md` 放仓库根目录；**PRD 内不出现技术名词**（技术选型留 Day 5–6）
- Day 4 产出：`PRD.md`（135 行 / 7998 字节 / 9 节）。验收标准 **12 条**；「本期不做」= research.md 13 条 + **PRD 阶段新砍 5 项**（行走动画 / 多语言 / 外观自定义 / 第二结局 / 加载进度条）
- 关键数值口径（自检后定稿）：一局 **3–6 分钟**（原 5–10 与文案量矛盾）；素材 **每张 ≤1.0 MB**、总量 ≤5 MB；素材实际计划 2 张（背景 + 角色）

## Day 5 技术方案定稿（用户拍板，2026-09-20）

- **技术路线（拍板 4A）：单文件 HTML + 原生 JavaScript / CSS**，DOM 绝对定位 + CSS `transition` 实现角色移动；图片作素材；托管 **GitHub Pages**
- **明确没有**：后端 / 数据库 / 构建工具 / API / 环境变量 / 账号系统 / 存档（均由 PRD 的「本期不做」推导而来，不是遗漏）
- 选型理由 4 条：① 交互只有"点地面移动 + 点物件"，无需逐帧渲染循环 ② 零构建，部署即 `git push`，与 Pages 天然咬合 ③ F12 可直接点中角色/物件，排错成本最低 ④ 体积几乎全由图片决定，预算留给美术
- 淘汰：Canvas 2D（要自己写渲染循环与命中检测）／ Vite + React（状态仅 4 个，多一层构建多一类错误）／ Phaser（引擎自带数百 KB，挤占图片预算）
- 技术栈浅：HTML + CSS + 原生 JS + 图片，仅此四样
- 关键技术判断：**角色坐标用相对百分比**表示（便于手机/电脑等比缩放，Day 6 用"等比缩放容器"处理窄屏）
- **新目录约定**：`images/`（Day 7 创建）放素材；素材文件名一律**英文小写 + 连字符**（Pages 托管在 Linux，路径大小写敏感）
- 部署路径：仓库 Settings → Pages → Deploy from a branch → `main` / `/ (root)` → 网址形如 `https://soren-yeyey.github.io/Soren2077/`
- 产出：`TECH_DESIGN.md`（**241 行 / 13820 字节 / 10 节**）。文档口径按用户拍板 **1A**：API、环境变量章节保留，写明"确认为空 + 为什么没有"
- 交图适配（用户拍板 **A**）：在第 5.1 节标题下加一行引用「本图所示的载体：单文件 HTML + 原生 JavaScript / CSS（技术路线见 1.1）」→ 第 5 节单张截图即可同时含数据流图与技术路线那一行
- Day 5 提交 hash：`f8224b05258b9e183e910bb9571d26c279a8887c`（4 文件 / 364 行新增 / 4 行删除）
- 数据流图用 **Mermaid**（用户拍板 3A）：`flowchart TD`，14 行图代码。**聊天窗口与本地编辑器不渲染 Mermaid，只有 GitHub 网页渲染** —— 需向用户预览时改用 SVG。**2026-09-20 已经用户截图实证：GitHub 页面上 Mermaid 正常渲染成图，节点/分支/虚线全在，方案成立、无需降级**
- 素材规格未定（用户拍板 5B，严格守清单）→ 留给 Day 6–7
- 文档内引用的平台事实（2026-09-20 查证）：Pages 站点 ≤1 GB（软）/ 带宽 100 GB/月（软）/ 单次部署 10 分钟（硬）；4G 下 5 MB 页面真实加载约 4–6 秒

## 已知环境异常（Day 2 发现，未解决）

- **本地写不进 `refs/remotes/origin/*`**：`git fetch` 与 `git update-ref` 都返回退出码 0 但引用不落盘，导致 `git status -sb` 显示 `## main...origin/main [gone]`、`git ls-tree origin/main` 报 `Not a valid object name`。
- 已排除文件系统因素（手动写 `.git/refs/remotes/origin/probe.txt` 可持久化）。
- **不影响 push / fetch / commit**。查远程事实一律用 `git ls-remote origin` 或匿名调 GitHub API，不要依赖 `origin/main` 这个本地引用。

## 已知环境异常（Day 4 发现，未解决）

- **本机代理对 `github.com` 转发异常**：系统环境变量指向本地代理 `http://127.0.0.1:52171`（`HTTP_PROXY` / `HTTPS_PROXY`，非 git 配置）。
  - `api.github.com` 走代理正常：HTTP 200，约 0.34s
  - `github.com` 走代理稳定失败：HTTP 000 / 超时 10s，`git push` 报 `CONNECT tunnel failed, response 502`（连续 3 次）
  - 绕过代理直连 `github.com:443` 亦不通（21s 超时）—— 属国内常态，不是解法
  - 判定：**代理服务端对 github.com 的转发/节点问题**，非 Git 配置或命令问题。
  - 应对：重试前先让用户检查代理（换节点 / 重启代理）。**查远程事实仍可用匿名 API 兜底**（`api.github.com` 通）。

## 远程查询手段优先级（Day 5 摸清）

1. **`git ls-remote origin refs/heads/main`** —— 最快，查编号用它
2. **`raw.githubusercontent.com/<user>/<repo>/main/<path>`** —— **逐文件探测 HTTP 状态码**（200 = 远程存在，404 = 不存在）。比 API 稳、**不限流**，适合确认"文件进仓库了没"
3. **`api.github.com/.../contents/`** —— 能列目录，但**未登录请求会被限流**（Day 5 实测返回 `API rate limit exceeded for 89.167.172.6`，出口 IP 为代理节点）。连续多次调用后会失效，不要作为首选
4. **不要依赖本地 `origin/main` 引用**（写不进，见 Day 2 异常）

> 另一条教训：`curl | grep -c` 在 for 循环里连续调用时可能个别次抖动返回 0。**单次异常要重试一次再下结论**，不要直接报给用户。

## 工作日志

- 2026-09-16：把用户指定的协作规则写入 `D:\workbuddy\AGENTS.md`（七节：任务边界、交互式推进、人机分工、讲清楚环节、Git 与提交、当日收尾、出问题的时候）。原 AGENTS.md 的环境准备类内容被替换。
