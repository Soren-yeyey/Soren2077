# 2026-09-23（Day 7 收尾 + v1 实机评价）

## Day 7 收尾

- 用户 09-23 15:44 回「今天做完了」。复核：本地 HEAD = 远程 main = `7fddbff…`；工作区只剩 2 个记忆文件脏（提交后补记，归 Day 8）；本地服务仍 HTTP 200
- 完成标准 3 条全达标；今日不做（登录/支付/缓存、一次生成大段代码）**未违规**
- **未收到的项**：带 `localhost` 地址栏的截图；手机实机验证（PRD 验收 1/6）；完整玩一轮计时（验收 9）→ 已在收尾时列为待确认

## 用户对第一版（v1）的实机评价（学习问题答案）

> 感觉人物移动比较机械，画面也一般

## 根因诊断（代码级，已核对 `index.html`）

### 「移动机械」

1. **`transition: left .7s ease-in-out, top .7s ease-in-out`（第 103 行）时间固定 700ms，与距离无关** → 点近处 3% 像瞬移，点远处 80% 像飞。真人走路速度应与距离成正比
2. **`ease-in-out` 是对称曲线** —— 起步慢/中段快/收步慢；真人走路是"起步加速 → 匀速 → 收步减速"，缺少分层缓动
3. **left 与 top 各 0.7s 同时插值 → 斜向移动实际速度是水平方向的 √2 ≈ 1.41 倍**。斜着走总比直着走快
4. **没有朝向** —— 角色是**单张背对镜头**的立绘，往左/往右都是同一张图，方向信息完全丢失。这是"机械感"最强的一条（棋子被推着走）
5. **没有步态、没有接触阴影** —— PRD 第 6 节 B 部分已砍「行走动画」，所以是"整张立绘平移"；脚下也无随位置变化的投影
6. 附带：`left/top` 是布局属性，每帧触发 layout + paint，低端机可能掉帧（**尚无实测证据，不下结论**）

### 「画面一般」

1. **角色"贴"在背景上** —— 角色是独立打光的剪影，背景是洗衣店实拍感光，两者光位/色温/景深不接；缺一层"大气"（雾 / 雨丝 / 光晕）把前后景粘起来
2. **没有透视缩放** —— y 从 58% 走到 90%，角色尺寸**一点不变**。走近镜头应变大，这一项缺失 → 空间感崩塌
3. **机位不动** —— 单机位单张图，无视差、无推拉
4. **画面是静态的** —— 除光圈呼吸外无任何持续动效：洗衣机不转、贩卖机灯不闪、雨不落

## 学习点

**功能循环 5 步就做完了（移动 / 物件 / 卡片 / 结束画面），而"让它像真的"的东西一样都不在里面。** 这不是偷懒，是顺序：先跑通"能不能玩"，再补"好不好玩"。第一版必然"能玩但糙"。这正是 PRD 砍掉「行走动画」的代价 —— 用它换了提前拿到能跑的东西。

## Day 8 候选改进清单（**未动手，等用户清单授权**）

| 改什么 | 动哪 | 收益 | 代价 / 风险 | 建议序 |
|---|---|---|---|---|
| 脚下接触阴影（椭圆投影，随位置轻微缩放） | 加 1 个 div + CSS | 站得住、不飘 | 几乎无 | 1 |
| 移动按距离计速 + 分段缓动（起步/匀速/收步） | 改 JS 十几行 | 立刻不"飘"、不"飞" | 无 | 2 |
| 走路时角色水平镜像 | JS 1 行 | 有朝向感 | 立绘是背面视角，镜像不完美但强于没有 | 3 |
| 环境动效（雨丝、滚筒转动、贩卖机灯闪） | 2–3 层 CSS 动画 | 画面"活"起来 | 需控性能；与"不做声音"不冲突 | 4 |
| 大气层（雾/光晕粘前后景） | 加 1 层叠加 | 去贴纸感 | 可能压暗物件，需重调色 | 5 |
| 按 y 做透视缩放（上小下大） | JS 加 scale | 空间感立起 | 缩太大时角色糊，须设上下限 | 6 |

**红线提醒**：「**行走动画**」在 PRD 第 6 节 B 部分**已被砍**（Day 4 拍板），要做步态必须**先改 PRD**、由用户拍板。上面 6 项均不在"本期不做"清单里，但**严格说都超出 Day 7 清单范围**，需 Day 8 清单授权才动手。

## 待办（转入 Day 8）

- 等用户 Day 8 清单；若清单涉及上表任一项，先报"是否需改 PRD"
- 用户仍欠：带 `localhost` 地址栏的截图、手机实机验（验收 1/6）、完整玩一轮计时（验收 9）
- Day 8 补文档：`TECH_DESIGN.md` 第 2 节项目结构与实际不符（`.jpg/.png` → `.webp`；新增 `RUN.md` 与 `.backdrop`；`isMoving` 字段未采用）
- 提交时带上 2 个脏记忆文件（`2026-09-22.md`、`MEMORY.md`）→ 归 Day 8
- 本地服务 **09-23 已停**（实测 HTTP 000），Day 8 板块②要重开

---

# Day 8（第 2 周）开工

## 清单原文要点

- 板块：① 一句话生成页面 ② 本地运行 ③ mock 数据渲染
- 主任务：生成并运行项目主视图（**mock 数据版**）
- 今日不做：**接真实 API（第 3 周才做）**；加登录
- 完成标准：页面本地能打开；**列表和卡片用本地假数据渲染**；改动已提交
- 截图：地址栏 + 已渲染出的列表或卡片
- 学习问题：四种页面状态里哪种最容易被忽略
- 余力加练：可复用卡片/列表组件；降级：只做静态数据版，保留加载/空/错误状态

## 用户附带的原始需求（他自己写的那句"一句话"）

> 根据 PRD 生成夜班 SOLITUDE 的首页前端；目标是让玩家眼前一亮，动态，色彩明亮的 AIGC 和美丽的人物建模；内容结构必须包含场景、人物、故事，还要有动态画面、可互动；视觉要求霓虹灯、不夜城、**蓝紫黑配色**，桌面和手机宽度都要能用。先说明结构和文件，再生成并给出验证步骤。

附 4 张参考图（**均为小红书带水印截图**，其中第 1、2 张含真实商标：TSUTAYA / COFFEE 等）。

## 我列出的 6 个冲突（A–F）

- **A** `PRD 路径.md` **不存在** → 全仓库无含「路径」的文件
- **B** 「列表 + 卡片 + mock 数据」在本项目**无对应物**（PRD 第 3 节只有 5 件事，无列表）；学习问题问的「四态」也是列表页形态
- **C** **美术方向冲突**：Day 7 是「暗色雨夜洗衣店 + 深色剪影」，参考图是「高饱和霓虹 + 二次元立绘」，两套美学不兼容
- **D** 清单「第 3 周接真实 API」与 `TECH_DESIGN.md` 已定的**无后端/无 API** 冲突 → 已提前报备，今天不动
- **E** 4 张参考图带小红书水印 + 真实商标 → **只作风格参考，不进 `images/`**
- **F** 本地服务已停（HTTP 000）

## 用户拍板（2026-09-23）

- **B①**：**新增一层「进店前街景首页」** —— 霓虹街口 + 角色 + 故事 + 动态雨夜 + 一排「今夜街上的动静」卡片（写死假数据），点店门进现有游戏。我的读法：可玩场景仍只有洗衣店 1 个，首页只作入口、不含走动 → **不算违反「不做多地图」**
- **C①**：**折中** —— 首页街景走霓虹不夜城（蓝紫黑、高饱和），进店后保持 Day 7 暖钨丝灯 + 深色剪影。**不改 `PRD.md` 第 9 节**，不废 Day 7 素材
- **A①**：按 `D:\workbuddy\PRD.md` 执行

## Day 8 拟定结构（待用户点头后生成）

- **不新建 HTML 文件**（守 §一.2）：在 `index.html` 内新增「街景首页」屏，**取代**原开场页（作品名 / 氛围文案 / 引导语 / 开始按钮 迁移过去），避免两层入口啰嗦
- **不新增角色图**：首页角色复用 `images/player.webp`（背影剪影站在霓虹街上成立）→ 省一张图
- **唯一新增素材**：`images/home-street.webp`（16:9 霓虹街景，含店门热区；目标 ~180 KB，≤1.0 MB）
- mock 数据 = JS 数组 → 渲染成卡片（不是硬编码 HTML）；卡片组件与现有 `.card` 复用
- 四态：加载（图未 decode 时的霓虹占位）/ 空（数组为空）/ 错误（`onerror` →「街区信号中断」+ 重试）/ 正常（4 张卡片）
- 验证开关提案：`?mock=empty` / `?mock=error` 用于现场演示三态（待用户批）
- 4 条街道短句**只是氛围条**，不含分支判定、不改结局 → 不违反 PRD「不做剧情分支树 / 第二结局」

---

# Day 8 执行记录（板块 ①②③ 已做完，待收尾提交）

## 用户追加拍板

> 做，同时也新增角色图

→ **推翻我的「不新增角色图」**。改为出 **2 张图**（街景 + 新角色），消耗已报备 10–20 credits。

## 出图与处理（实测数据）

| 图 | 原图 | 处理后 | 体积 |
|---|---|---|---|
| 街景 | 1536×1024（水印 y 963–1013） | 裁顶 50 + 裁底 110 → 1536×864（精确 16:9）→ LANCZOS 放大 **1920×1080** | **243.3 KB** |
| 角色 | 1024×1536（水印 x 563–1013 / y 1311–1525，落在纯黑底） | 水印填黑 → `alpha=clip((L-12)/16)` → 反预乘去黑边 → 裁紧 **599×1501** | **205.5 KB** |

- 角色这次是**亮色**（浅银发 + 奶白外套），与 Day 7 的深色剪影相反；背景四角亮度仅 1.5–8.1，alpha 曲线沿用 `(L-12)/16` 就够
- **关键新经验**：亮色角色抠图后要**反预乘去黑边**（`rgb / max(alpha,0.02)`），否则霓虹边光的半透明像素会带一圈脏黑
- 街景 prompt 写 `no readable text` 而不是 `no signage` —— 霓虹街景必须有招牌，但不能有可读文字

## 代码改动（`index.html` 487 → 957 行 / 18521 → 34912 字节）

- 删掉 `#intro`（开场页），**替换为 `#home` 街景首页**；作品名 / 氛围文案 / 引导语全部迁到首页顶栏
- **首页也用 16:9 舞台**，不用 `object-fit: cover` 铺满视口 —— 原因：店门热区是图上的百分比坐标，`cover` 会裁切图片，百分比就对不上了。**这条是今天最重要的结构判断**
- 首页元素：`.home-bg`（图）/ `.home-rain`（雨）/ `.home-scrim`（上下压暗，放 `#home` 层跟视口）/ `.home-top`（顶栏，跟视口）/ `#playerHome`（角色）/ `.home-door`（店门热区 x 52%–77% / y 50%–78%，在成品图上标定）/ `.home-panel`（底部列表，fixed 跟视口）
- 两屏切换：`#home.hidden` ⇄ `#stage.active`；`.backdrop` 的图由 JS 在两屏之间切
- 四态用 `#homePanel[data-state]` 切（loading / empty / error / ok），**不写 4 套 DOM**
- 三个验证开关：`?mock=loading|empty|error`。**loading 是后补的** —— 900ms 定时器用"手快抓拍"抓不到，改成可停留的稳定入口

## 实测（headless Chrome，全部通过）

| 项 | 结果 |
|---|---|
| 正常态 | `state=ok`、DOM 里 **4** 个 `.note`；`index.html` 源码里列表容器是**空的 `<ul>`** → 确认是 JS 渲染而非写死 |
| 空 / 加载 / 错误 | 三个开关分别稳定得到 `empty` / `loading` / `error`；错误态带「重新连接」按钮 |
| 点卡片 | 弹出，内容 `02:14　对面便利店的灯还亮着，没人进去。`；点卡片外收起 |
| 首页走动 + 越界 | 点 (2%,30%) → **10%/52%**；(90%,95%) → **46%/76%**（收敛到 HOME_WALK） |
| 点店门 | `home.hidden=true`、`stage.active=true`、进度 show、垫底图换成 `scene-laundromat.webp` |
| 店内走完 → 再来一次 | 计数 3 → 结束画面 → 回**街景首页**、计数 0、首页角色复位 34%/74% |
| 手机竖屏 390×844 | 舞台 390×219、面板高 243、卡片 **2 列**（178×78）、**无横向溢出** |
| 宽屏 1920×800 | 舞台 1422×800，左右留白各 249px（模糊垫底填） |
| 报错 / console | 均为空 |

## 雨的效果调了三次（值得记）

1. 间隔 7px / alpha .15 → 糊成一层灰网，把画面盖住
2. 12px / .085 → 仍像纱窗，且**满屏均匀**，连最远的楼都挂着雨丝，一眼假
3. **20px / .07 + 58px / .03，并加一道纵向 mask**（上半透明 → 下半实心）→ 近处雨清、远处雨淡，有纵深

## 顺手做了但需报备

`RUN.md` 加了一节「九、三个验证开关」，并更新「怎么玩」「文件清单」（改 4 张图）；原「九、出问题先查这里」顺延为「十」。理由：新增的开关与两张素材属运行说明范畴，不更新就出现"文档与实现不一致"（正是 Day 7 记下的 TECH_DESIGN 遗留问题类型）。**不认同可撤**。

## 用户报「网页显示出错」→ 根因：localhost 解析成 IPv6，服务只绑了 IPv4

**症状**：用户打开 `http://localhost:8000/` 打不开（我这边无头 Chrome 一直正常 → 典型「我这边好、你那边没好」）。

**根因（实测）**
- 本机 `socket.getaddrinfo('localhost')` → **`('::1', 8000, ...)`，优先 IPv6**
- 我起的服务是 `--bind 127.0.0.1`（**仅 IPv4**）
- `http://127.0.0.1:8000/` → HTTP 200 ✓ ；`http://[::1]:8000/` → **HTTP 000** ✗
- 无头 Chrome 会 Happy-Eyeballs 回退到 IPv4，所以**我的自动化测试全绿，用户全红**

**解法（已验证）**：`python -m http.server 8000 --bind ::`
- 本机 Python 3.13 下 `--bind ::` 是**双栈**：netstat 同时出现 `0.0.0.0:8000` 与 `[::]:8000`
- 验证：`localhost:8000` **HTTP 200**、`127.0.0.1:8000` **HTTP 200**（两个都通）
- 附带好处：手机走局域网 IPv4 也走同一条命令，**不再需要 `--bind 0.0.0.0`**

**已同步文档**：`RUN.md` 第二节主命令加 `--bind ::` + 解释小字；第六节手机那节把 `--bind 0.0.0.0` 换成 `--bind ::`；第十节排错表加一行「浏览器说无法访问 → 先看是否漏了 `--bind ::`，换 `127.0.0.1` 能打开即可确认」。

**教训（重要）**：**验证必须用用户实际会用的地址**。我一直用 `127.0.0.1` 或无头 Chrome 测，等于绕开了用户真实的失败路径。以后交付本地页面，**通信地址一律以 `localhost` 为准做实测**。

## 用户拍板：统一角色（店内也换成新立绘）

- `index.html` 第 586 行 `#player` 的 `src`：`images/player.webp` → **`images/player-neon.webp`**
- 实测：解码 **599×1501**，渲染 **65×162**，宽高比 **0.3990** vs 素材 **0.3991**（**未变形**），占舞台高 **26.0%**
- 两屏现在是同一个人（银发 + 奶白外套 + 青紫双色边光）
- `images/player.webp`（174,124 B）**闲置但保留** —— 删了 Git 历史里那份也清不掉，留着不占额外体积
- 截图复核：店内一屏（新角色站在洗衣机前，与暖光场景协调）、结束画面「天快亮了」均正常；`errors` / `console` 空

## 顺带发现的两件事

1. **代理端口变了**：Day 4 记录的是 `127.0.0.1:52171`，现在是 **`127.0.0.1:52327`**（用户重启过代理）。`github.com` 仍失败（一次 `Connection was reset`、一次 21s 超时），`api.github.com` 正常 → 查远程事实继续用匿名 API 兜底。
2. **Git Bash 里调 taskkill 的写法**：`//PID` 会报「无效参数/选项」，正确写法是 **`MSYS_NO_PATHCONV=1 taskkill /PID <pid> /F`**。

## 待办（转入 Day 9）

- **收尾**：列改动清单 → 用户确认 → 提交 `Day 8｜…`（带上遗留的 2 个脏记忆文件）
- **用户要交的截图**：带地址栏 + 已渲染出的卡片
- **Day 8 学习问题未答**：四种页面状态里，哪种最容易被忽略
- **余力加练**（清单提及，未做）：让 AI 生成一个可复用卡片或列表组件
- **Day 7 遗留仍未闭环**：带 `localhost` 的截图、手机实机验（PRD 验收 1/6）、完整玩一轮计时（验收 9）
- Day 9 补文档：`TECH_DESIGN.md` 第 2 节项目结构与实际不符（`.jpg/.png` → `.webp`；新增 `RUN.md` / `.backdrop` / `#home` 屏与两屏结构；`isMoving` 字段未采用）
- 用户 Day 7 提的「移动机械 / 画面一般」6 项改进清单（见本文件上方）**仍未动手**
