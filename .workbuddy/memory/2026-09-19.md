# 2026-09-19（Day 4）

## 今日清单（Day 4｜第 1 周）
- 板块顺序：① PRD 结构讲解 → ② 撰写 PRD → ③ AI 自检
- 主任务：完成 PRD.md 并让 AI 按验收标准自检一次
- 今日不做：技术选型（Day 5）、写代码
- 完成标准：PRD.md 已入库；含验收标准；AI 自检挑不出「必须修改」级问题
- 交图：打开的 PRD.md，图里要有**验收标准那一段**，并能看出是哪个项目
- 收尾：列改动清单（含归属天数）→ 提交推送（`Day 4｜…`）→ 停下等确认
- 余力加练：让 AI 把一项功能写成用户故事
- 卡住降级：只完成"首页、详情和一项写入功能"的 PRD，其他内容列后续（**与游戏项目不匹配，按精神理解**）

## 开工只读探测
- 工作区：`.env` / `.gitignore` / `AGENTS.md` / `index.html` / `research.md`，**无 PRD**
- 本地 HEAD 与远程 main 均为 `033caed05c66f7e2d455cff70a9181e7a75726e1`，完全同步
- 遗留改动：`.workbuddy/memory/2026-09-18.md`（Day 3 收尾记录，写于上次 commit 之后）
- `origin/main` 书签异常**第四次确认仍存在** → 停止复查，只用 `git ls-remote`

## 用户拍板（2026-09-19）
- 作品名：**夜班 SOLITUDE**
- 玩家能做什么：**2A** —— 移动 + 点 2–3 个物件看剧情
- 目标/结束：**3B** —— 轻目标，点完 3 个物件触发结束画面
- 开场引导：**4A** —— 一句文字提示
- 降级条款理解：按 A（精神一致），用户未明确表态，已在计划中说明

## PRD 计划结构（8 节）
```
1. 项目一句话 + 作品名      2. 目标玩家与使用场景
3. 玩家能做什么（功能清单）  4. 玩家看到什么（界面与流程）
5. 验收标准 ★              6. 本期不做（继承 research.md 13 条）
7. 依赖与前提              8. 未决问题
```
- 落点：`D:\workbuddy\PRD.md`（根目录，与 research.md 并排）
- 硬规则：PRD 内**不出现技术名词**（不写框架、不写实现方式）

## 板块① 完成（PRD 结构讲解）
- 讲了 PRD 在五步工作流**第 2 步**的位置、与 research.md 的分工、**PRD 不写技术**这条界限
- 配图：PRD 内容边界（要写四件 / 坚决不写四件）
- 学习问题已抛出（待 PRD 落盘后作答）
- 顺手落定（用户选 A）：`index.html` 的 `<title>`/`<h1>` 由 `Soren2077` 改为 `夜班 SOLITUDE`，288 → 300 字节

## 板块② 完成：PRD.md 已落盘
- 路径 `D:\workbuddy\PRD.md`，**132 行 / 6885 字节 / 9 节**
- 流程：先给完整草稿（未落盘，创作决定标「提案」）→ 用户回「可以写」→ 落盘并把「提案」改为「已定」
- 内容要点：
  - 场景设定：**雨夜街角自助洗衣店（24H）**，方案 A；B/C/D 备选作为决策记录留在第 9 节
  - 三个物件：还在转的洗衣机（没人取的外套）/ 自动贩卖机（停产饮料）/ 老式收音机（夜班被自动化取代的新闻）
  - 验收标准 **10 条**，全部可当场验证（含手机端、控制台报错、加载体积 ≤5MB、时长 5–10 分钟、商标合规）
  - 本期不做 = research.md 13 条 + **PRD 阶段新砍 5 项**：行走动画 / 多语言 / 外观自定义 / 第二结局 / 加载进度条 ← **今日学习问题的答案**
- 硬规则遵守：全文无技术名词（已 grep 验证：无 Canvas/three.js/框架/API 等）

## 板块③ 完成：AI 自检 → 修改 → 重检
- 首次自检结论：**必须修改 5 条 / 建议修改 5 条 / 可忽略 2 条**（不达标）
- 用户拍板：**1A**（时长 5–10 → **3–6 分钟**）、**2A**（素材每张压缩到 **≤1.2 MB**）
- 已按方案修改 `PRD.md`：135 行 / 7939 字节；验收标准 10 → **12 条**（新增第 11 条：结束画面行为；第 12 条："再来一次"）
- 重检结果：5 条必须修改全部落实，「5–10 分钟」全篇清零，已读物件/结束后交互/卡片关闭均写明 → **「必须修改」归零，达标**
- 重检新发现（建议级 1 条）：第 7 节「每张 ≤1.2 MB」与「素材 3–5 张」相乘，5 张时 = 6 MB > 第 8 条总上限 5 MB，措辞不严谨；建议单张上限改为 **1.0 MB**（**已问用户，尚未修改**）

## 重检发现已修（用户回「改」）
- 第 7 节单张素材上限 **1.2 MB → 1.0 MB**，并补注算术依据（5 张 × 1.0 = 5.0 MB，最坏情况也不越线）
- 重检通过：无 1.2 MB 残留；验收标准仍为 12 条；`PRD.md` 135 行 / 7998 字节
- 至此「必须修改 0 条 + 建议修改 0 条」，板块③ 全部收口

## 待办
- 待用户作答学习问题（PRD 里砍掉了什么、为什么砍）
- 收尾：等「今天做完了」→ 逐条核对表 + 群里模板；交图对象 = PRD 第 5 节验收标准（12 条）
- 本次待提交 5 项：`PRD.md`(Day 4) / `index.html`(Day 2 遗留 + Day 4 命名统一) / `2026-09-19.md`(Day 4) / `2026-09-18.md`(Day 3 遗留) / `MEMORY.md`(Day 3+4)；`.env` 仍被忽略
