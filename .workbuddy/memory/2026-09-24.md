# 2026-09-24（用户口径 Day 9 · 第 2 周）

## 今日任务清单（用户原文要点）

- 主题：**用设计规则完成一次前端审查和修复**
- 板块顺序：① 了解设计规则 → ② 用规则做前端审查 → ③ 修复
- 检查六项：信息层级、字体大小、颜色对比、卡片间距、按钮状态、手机宽度布局
- 完成标准：至少修掉 3 处设计问题（文字对比 / 间距 / 对齐等）；修前后截图各一张（**要有地址栏**）
- 今日不做：大改版面结构；新增页面
- 红线：不加渐变背景、随机装饰、与产品无关的组件
- 收尾：列改动文件清单（标注属哪天任务）→ 提交推送（commit 标题 `Day 9｜一句话`，说明两行：改了什么 / 加了什么）→ 停下等确认
- 余力加练：把一条视觉规则写成可复用组件约束
- 卡住降级：只修文字对比、间距、移动端溢出三项

## 编号澄清（用户 19:36 确认）

- 用户：「昨天是 day8，我打错了，今天是 day9」
- **冲突事实**：9-23 当天已推过 5 个 `Day 9｜…` 提交（`660ac70` / `41dce83` / `4968c86` / `c1e1bbd` / `42b1116`），那批其实是 Day 8 的追加
- **处置**：已推送历史不改（改需强推，AGENTS.md 禁止）；今天的提交标题按用户说的仍用 `Day 9｜`
- 项目编号对齐：Day 7 = 9-22（`7fddbff`）、Day 8 = 9-23（`f3c7047` + 5 个追加）、Day 9 = 9-24

## ① 设计规则来源（用户拍板 A）

- **探测结论**：项目内**无成文设计规则**（`PRD.md` 无视觉章节、`AGENTS.md` 第八/九节只管素材命名与文案）；技能市场（SkillHub）搜「ui design review / 前端设计规范」**无对口 Skill**；本地 `design-suite` 只含 Figma 导入；可用的最近工具是 `dev-suite/headless-ui-pixel-verify`（像素级 UI 验证）
- **拍板 A —— 可量化通行规则**：
  1. 对比度 WCAG AA：正文 ≥4.5:1、大字（≥18.66px 粗体 / ≥24px）≥3:1
  2. 触控目标 ≥44×44px（iOS HIG）/ ≥48dp（Material）
  3. 正文 ≥14px；任何文字不低于 12px
  4. 间距用 4/8px 尺度，不用 7px / 9.284px 这类碎值
  5. 层级靠字号 / 字重 / 颜色区分，字号阶梯至少差 4px 才算一档
  6. 交互元素必须有按下（active）与聚焦（focus-visible）状态；不能只靠 hover（移动端无 hover）

## ② 前端审查（实测，无头 agent-browser）

### 实测字号（手机 390×844）

| 元素 | 字号 | 判定 |
|---|---|---|
| 顶栏 `.eyebrow` | **9px** | ❌ 低于 12px |
| `h1` | 20px | ✅ |
| `.tagline` | 11px | ❌ |
| 面板标题 `.panel-head h2` | 11px | ❌ |
| 面板计数 `.panel-head em` | **9px** | ❌ |
| `.note time` | **9.5px** | ❌ |
| `.note p` | 11px | ❌ |
| `.note-next` | **9.5px** | ❌ |
| `.home-hint` | **9.5px** | ❌ |
| `.home-door span` | **9.5px** | ❌ |
| `#progress` | 12px | 临界 |
| `#cardText` | 13.5px | 临界 |

### 对比度（按已知 CSS 层合成计算）

**12 处全部达标**，最低 5.13:1（`.home-hint`）、`.note-next` 5.2:1，其余 6–18:1。

> **结论：「文字对比」不是本项目的问题** —— 如实记录，不为了凑数硬修。

### 触控热区（手机）

- `.hotspot` = **37×37px**（`width: 9.5%` × 390 舞台宽）→ **低于 44×44 下限**
- `.home-door` 98×61 ✅、`.note` 364×78 ✅

### 间距 / 盒模型（手机）

- **`.note` 自身 `font-size` = 16px**（继承 body，未随内部小字号收），所以 `padding: .68em .85em .72em` 实际算出 **10.88 / 13.6 / 11.52px** —— 按 16px 基准算的，而卡片内正文只有 **11px** → **同一张卡片里两套 em 基准**
- `.note-next` margin-top = **3.99px**（按自身 9.5px 算）
- `.card-list` gap = **7px**（非 8px 尺度）
- `.note time` `line-height: normal`
- `.card-list` gap / `.panel-head` margin-bottom = 9.284px（碎值）
- **手机无横向溢出**：`scrollWidth = 390 = innerWidth` ✅

### 按钮状态

- `.btn`：有 hover / active ✅
- `.retry`：**无 hover、无 active** ❌
- `.home-door`：有 hover / focus-visible，**无 active** ❌
- `.hotspot`：有 hover / focus-visible，**无 active** ❌
- 关键：`.btn` / `.retry` 的反馈**只挂在 `hover` 上** → 移动端完全无按下反馈

## 问题清单（5 条，按影响排序）

| # | 问题 | 依据 | 涉及检查项 |
|---|---|---|---|
| 1 | 手机端 9–11px 小字号泛滥；且 9 / 9.5 / 11 三档挤在 2px 内，字号阶梯事实上无效 | 实测 8 处 <12px | 字体大小 / 信息层级 |
| 2 | 可互动物件触控热区 **37×37px** < 44px，手指点不准 | 实测 `.hotspot` | 手机宽度布局 / 可用性 |
| 3 | 按钮按下反馈缺失：`.retry` 无 hover/active；`.btn`/`.retry`/`.home-door`/`.hotspot` 移动端无 active | CSS 无 `:active` 规则 | 按钮状态 |
| 4 | 间距基准错位：`.note` padding 按 16px 算、内部文字 11px；`.note-next` 3.99px；时间与正文仅隔 4.18px；gap 7px / 9.284px 碎值 | `getComputedStyle` | 卡片间距 |
| 5 | 面板标题与正文同为 11px，仅靠字重 500 区分，手机上级层不明显 | 实测同字号 | 信息层级 |

## 当前状态 / 下一步

- **等用户截「修复前」截图（必须含地址栏）** —— 我一旦动手改，那个状态就补不回来
- 本地服务已在跑：`python -m http.server 8000 --bind :: --directory D:/workbuddy`（双栈，`localhost:8000` 可通）
- 修复范围：仅上述 5 条；**不动版面结构、不加装饰**
- 回归手段：改完跑一遍线上三幕流程（点物件 → 三幕顺序 → 结束画面）+ 无头四档视口量测

## 插曲：用户发来一张截图（19:42）→ 顺着查出两处真问题

- 截图是微信对话 + 桌面浏览器里的**店内**画面（那句「好像出了点问题」就在对话里）。**先复现再判断，不猜**
- 按用户窗口比例复现（视口 1117×1040）：三个物件中心 37%/56%、92%/33%、5%/21%，角色脚底 78% —— 与用户截图**逐像素对齐**；把用户截图（原图 1920×1200）的页面区与我的复现做灰度差分，**中位差异 2/255**，只有右下角窗口边缘不同 → 渲染完全一致
- 交互也全正常：点地面 (0.2,0.85) → 19.96%/84.86%；越界收敛 (0.98,0.95)→94%/90%、(0.02,0.05)→6%/58%；点物件 → 卡片出现、进度 0→1→2；零报错
- ⚠️ **踩了自己一个坑**：地面点击的监听挂在 `#scene` 上，我先发事件给 `#stage` → 角色不动，一度以为"点地面失灵"。**测点击要先 `elementFromPoint` 拿真实命中元素，再往上发事件**
- 用户确认「**没问题，误会了**」（微信那句是虚惊）
- 顺着查出**两处真问题**，用户拍板「**两处都改**」：
  1. **首屏多下 176KB**：街景阶段就把店内背景图 `scene-laundromat.webp` 下了（首屏 626KB 里 28% 是"还没进店就用不上"的）
  2. **文字层视口单位不一致**：页面别处统一用 `--vh`（= `dvh`），卡片那几处仍是原生 `vh` —— iOS 微信 / Safari 的 `100vh` 比可见区域高，卡片按更大的高度算

## 改动（2026-09-24，未提交）

| 文件 | 改动 |
|---|---|
| `index.html` | ① `vh` → `var(--vh)` 共 **10 处**（`#card` / `#cardText` / 矮视口媒体查询共 5 处 + `.home-top`、`.home-panel`、`.panel-head`、`.panel-state`、`.home-hint` 5 处 `clamp`）② 店内背景图 `src` → `data-src`，新增 `loadStoreBg()`：`window.load` 后借 `requestIdleCallback` 预取，`enterShop()` 里兜底 |
| `.workbuddy/memory/MEMORY.md` | **压缩重写**（351 → 约 130 行）：原文件超限被截断，只保留跨天有效的约定、定稿与可复用的坑，日细节交给当日日志 |

### 验证

- **延后加载（时序硬证据）**：`loadEventEnd = 86ms`，而 `scene-laundromat.webp` 的 `startTime = 166ms` → **在 load 事件之后才发起**；进店前 `bg.complete = true` / `naturalWidth 1920` → 不会闪白
- **兜底路径**：手动 `removeAttribute('src')` 后从街上再进店 → src 被补回并加载成功
- **回归（三档视口，与改前数值完全一致）**：手机 390×844 卡片 295px / 34.9%；横屏 844×390 降级 148px / 38%（配图隐藏）；桌面 1440×1080 366px / 33.9%、文字区 172.2px；`--vh` 解析为 `100dvh`；无横向溢出；`errors` / `console` 零输出
- ⚠️ **诚实记录**：这一处在 Chromium 上数值**完全不变**（0.34×624=212 仍被 296px 下限吃掉），它修的是 iOS 那套 `100vh > 可见高度` 的行为 —— **本机没有 iOS 环境，给不出改前改后的对比数字**，只有机制与代码一致性证据

## 提交与推送（20:14 ~ 20:22，已完成）

- 提交 **`d5ff8f5`**：`Day 9｜统一视口单位口径，并把店内大图推迟到进店前加载`（4 文件 / +269 −357）
  说明两行：改了什么 = 10 处原生 vh 改回 `var(--vh)`；加了什么 = 店内图 `data-src` + 空闲预取 + 进店兜底，首屏 626KB → 450KB
- **推送过程撞了代理故障**：`git push` 连试 5 次全败，报 `schannel: failed to receive handshake`，换 OpenSSL 后端报 `unexpected eof while reading`
- 诊断（可复用的判别法）：
  - 走代理访问 `baidu.com` → **200**，代理自身工作正常
  - `api.github.com` → CONNECT 隧道 **200 Established**，随后 TLS 断
  - `raw.githubusercontent.com` / `github.io` → 全 **000**
  - 本机 `github.com` 解析到 **`198.18.0.37`**（代理的 fake-IP 段）→ "绕过代理直连"天然走不通，只能走代理
  - 结论：**代理软件当前节点对 GitHub 的线路坏了**，与 Git 配置/命令无关
  - 正解：请用户换节点 / 重启代理 → 恢复后**一次推送成功**（`42b1116..d5ff8f5`）
- **远程核验**：远程 `main` = `d5ff8f56bdb6931e08f53d2ea8a0e47641808662`
- **线上核验**（Pages 构建约 50 秒）：`index.html` 去 CR 后 md5 本地/线上**同为 `0feb07a545819ec1d68b24caff0363a7`（64298 字节）**；剥掉注释后裸 `<数字>vh` **只剩 `:root { --vh: 100vh; }` 这一处变量定义**，`var(--vh)` 实际使用 **14 处**；`data-src` / `loadStoreBg` / 进店兜底各 1 处，全在

## ③ 修复（20:30 ~ 21:00 已完成，未提交）

用户拍板「A」——用**可量化通行规则**做审查与修复。5 条问题全部动手，另加 2 处审查时才暴露的隐患。

### 修法（逐条）

| # | 问题 | 修法 |
|---|---|---|
| 1 + 5 | 8 处字号 <12px；9 / 9.5 / 11 三档挤在 2px 内；标题与正文同为 11px | **建立四档字号阶梯 12 / 16 / 20 / 24**，每档差 4px，全页 `font-size` 只从这四档取（大标题档保留 `clamp` 上限缩放）。`.panel-head h2` 11 → **20px**，与 `.note p`（16px）拉开一档 —— 层级从此由字号+字重+颜色三者共同表达 |
| 2 | `.hotspot` 热区 37×37px | 加 `.hotspot::after { inset: -8px }`，命中区 **37 → 49px**，光圈视觉尺寸**一点不变**（不能直接改 width，那会动到 Day 7 标定的构图） |
| 3 | `.retry` 无 hover/active；4 个可点元素移动端无 active | 新增 CSS 第 ⑨ 节**统一约束**：`:is(.btn,.retry,.note,.home-door,.hotspot)` 三条 —— ① 命中区 ≥44px ② `:active` 立刻提亮 ③ `:focus-visible` 同一焦点环。`.btn:hover` 补进 `@media (hover:hover)`，`.btn:active` 由"取消上浮"改成 `scale(.97)` |
| 4 | `.note` 两套 em 基准；7px / 9.284px / 3.99px 碎值 | `.note` 显式 `font-size: 16px` + `padding: 12px 16px`；`.note p` `margin-top: 8px`；`.note-next` `4px`；`.card-list` `gap: 8px`；`.panel-head` `margin-bottom: 8px`；`.home-hint` `margin-top: 8px` 并补 `line-height` |

### 审查时才发现的 2 处（不在原清单里）

- ⚠️ **`<button>` 不继承 body 字号** —— 浏览器默认 **13.3333px**，实测 `.home-door` 与三个 `.hotspot` 都落在它上面，等于绕过了整条阶梯。基础节加 `button, input, select, textarea { font: inherit }`
- 其余 em 间距的碎值：`h1` `margin-top` 10.08px、`.tagline` 11.2px、`.home-door span` padding 4.08 / 10.8px、`.searchline` 14.4px → 全部改成 4/8 尺度

### 验证（无头 agent-browser，实测数据）

- **字号集合**：手机 390×844 / 横屏 844×390 / 桌面 1440×1080 三档全部只剩 `{12, 16, 20, 24}`（横屏与桌面另有 h1 的 30.384 / 44px 与结束标题 40px，属大标题档的 clamp 缩放，正常）
- **热区**：光圈 `37×37` 不变；`::after` 实测 **49.05×49.05**；在光圈边缘往外的 6px 处 `elementFromPoint` 取到的是 `BUTTON.hotspot` —— 伪元素确实在吃事件
- **横向溢出**：三档均 `scrollWidth == innerWidth`（390/390、844/844、1440/1440）
- **手机几何**：街景舞台 312.3→531.7；面板上沿 651.9、高 192.1 → **离舞台还有 120.2px，没压到画面**；提示行单行 17px
- **卡片**：手机 294.8px 高、文字区 `max-height: 101px`、正文 16px / 行高 27.2px，内容 464px 需滚动（逐句浮现 + 自动滚动照旧工作）
- **完整流程**：进店 → 三幕依次点完（进度 1/3 → 3/3）→ 点空白收起 → 结束画面（标题 24px、正文 16px、按钮 16px / 171.2×51.2）→ 再来一次 → 回街景、进度归零、三个光圈复位 `未读`。`errors` 与 `console` **零输出**
- **横屏降级**：卡片 148.2px（= 0.38×390）、配图已藏、文字区上限 108.2px；光圈 66px / 命中 77.86px
- 截图三张（无头，无地址栏）存 `Temp/`：`fix-after-mobile-home.png`、`fix-after-mobile-shop.png`、`fix-after-desktop-home.png`

### 记录在案、**本次没改**的一处

- **横屏 844×390：面板高 182.9px，整块压在舞台下半部分**（改前约 138px，多盖 45px）。
  桌面 1440×1080 面板压舞台 65.9px，与改前基本一致，属既有设计（CSS 注释里已声明接受）。
  横屏这处是"字号达标"的直接代价，收紧它属于版面结构调整（用户清单今天明确不做）→ **留给用户定**。
  可选做法：在 `@media (max-height: 520px)` 里只收紧 `.home-panel` 与 `.note` 的内边距（约省 20px），**不动字号**（12px 是下限）

## 提交与推送（21:15，已完成）

- 提交 **`6ce8162`**：`Day 9｜按可量化设计规则修掉 7 处前端问题`（3 文件 / +193 −41）
  说明两行：改了什么 = 字号四档 / 热区 37→49px / 补 active 与 focus-visible / 4·8 间距 / button 继承字号；加了什么 = CSS 第 ⑨ 节「可交互元素统一约束」
- 推送：首次 `git push` 被中断（`Exit Code 1 / SIGTERM`，**不是**代理故障）→ 先探连通性（`baidu.com` 200、`github.com` 200、`api.github.com` 403 属限流）→ **重试一次即成功**（`d5ff8f5..6ce8162`）
  - 判据补充：**SIGTERM / 空输出 与 TLS 握手报错是两类问题** —— 前者直接重试，后者才要请用户换节点
- **远程核验**：远程 `main` = `6ce81625fc9229b87ca15ae34a897ac345e1e1e8`
- **线上核验**（Pages 构建约 50 秒）：`index.html` 去 CR 后 md5 本地/线上**同为 `08debfb647761c7f4341741e145cef45`（71700 字节）**；抽查 8 项两边全一致 —— `font: inherit` 1、`inset: -8px` 1、`:is(.btn` 3、`font-size: 16px` 8、`12px` 7、`20px` 1，而 **`9px` 与 `11px` 两边都是 0**（小字号彻底清除）；`<title>夜班 SOLITUDE</title>` 两边一致
  - 命令教训：`grep -oF` 是**固定字符串**匹配，写 `'<title>[^<]*</title>'` 永远匹配不到 —— 要正则就用 `-oE`

## 「修复后」截图已收到（21:28）

- 用户发来两张手机截图，**均带地址栏**（`soren-yeyey.github.io`）：
  - 街景首页：顶栏「交互式短篇 · 第 01 夜 / 夜班 SOLITUDE / 凌晨三点，雨还没停。洗衣店的灯还亮着。」→ 街景画面 +「推门进去」→「今夜街上的动静 **1 / 10**」单条卡片（02:14）→ 底部三句操作提示
  - 店内：「已读到 **0 / 3 幕**」，三个光圈 + 角色就位
- 目视核对：字号明显大于修复前、三级层级可辨（大标题 ↔ 正文 ↔ 提示行）、无重叠、无横向溢出、无装饰跑偏
- 与「修复前」两张凑成完整前后对比组 → **今日清单全部闭环**

## 下一步

- 待用户定：横屏 844×390 面板从 138px 涨到 182.9px、多盖舞台 45px，要不要在矮视口媒体查询里收紧内边距（属版面结构调整，今天清单明确不做）
