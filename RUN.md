# RUN.md — 夜班 SOLITUDE 怎么跑起来

这份文件只回答一件事：**这台电脑上，怎么把它打开来玩。**

---

## 一、这是什么

一个 AIGC 风格的网页互动小游戏，一共两屏。玩家先站在雨夜的霓虹街口，看几眼街上的动静，推门进一间深夜自助洗衣店，在店里走动、点三个物件看三段短剧情，看完出结束画面。

- 形态：**一个 `index.html` 装下全部内容**（结构 + 样式 + 逻辑），加四张图片素材（街景 / 店内，两屏各两张）
- 没有后端、没有数据库、不需要构建、不需要装任何依赖
- 联网后打开一个网址就能玩，电脑和手机都可以

---

## 二、跑起来（电脑）

**前提**：装了 Python 3（本机已装 3.13）。

1. 打开命令行（PowerShell 或 CMD 都行）
2. 粘这两行：

```
cd /d D:\workbuddy
python -m http.server 8000 --bind ::
```

3. 浏览器打开 → **http://localhost:8000/**
4. 想停下来：回到命令行窗口，按 **Ctrl + C**

> **末尾那个 `--bind ::` 别省。** 本机把 `localhost` 解析成 IPv6 地址 `::1`，不加这个参数，服务只监听 IPv4，浏览器打开 `localhost:8000` 会**连不上**（只有 `127.0.0.1:8000` 能通）。加上它，两种写法都通。PowerShell 里如果报错，写成 `--bind "::"`。
>
> **已经起过了？** 直接打开 http://localhost:8000/ 就行，服务可能还在后台跑着。
>
> **报 `Address already in use`** → 端口被占，把上面那个 `8000` 换成 `8001`，网址也跟着改成 `http://localhost:8001/`。

---

## 三、为什么不直接双击 index.html

- 双击打开走的是 `file://` 协议，图片路径和浏览器安全策略都会出问题
- 用本地服务器（`http://`）才是**和上线后一模一样**的环境，本地能开＝线上也能开
- 交图要求地址栏是 `localhost` 开头，也就是这个理由

---

## 四、怎么玩

**第一屏：街景（进店前）**

1. 打开就是雨夜的霓虹街口。作品名在左上，底部一栏「今夜街上的动静」
2. **点地面** → 角色走过去
3. **点底部任意一张卡片** → 弹出一句，点卡片以外的任何地方收起
4. **点「推门进去」**（洗衣店的玻璃幕墙那块）→ 进店

**第二屏：洗衣店（店内）**

5. **点地面** → 角色走过去。可站立范围是收敛过的，点四角或点到画面外也不会走出画面
6. **点三个发光的圆环**：
   - 左上 —— 老式收音机
   - 中间 —— 还在转的洗衣机
   - 右侧 —— 自动贩卖机
7. 弹出的文字卡片：**点卡片以外的任何地方**收起
8. 三个都看过 → 画面渐暗，出现结束画面 → 点「再来一次」重开（回到街景首页，三个物件恢复可点）

一局约 3–6 分钟。

---

## 五、改完文件怎么看到效果

| 改了什么 | 怎么生效 |
|---|---|
| `index.html` | 浏览器按 **F5** 刷新；没变化就 **Ctrl + F5**（强刷，绕开缓存） |
| `images/` 里的图 | 同上（换了图也要强刷） |

**改图时的两条硬规矩**：

1. 文件名**只用英文小写 + 连字符**（如 `scene-laundromat.webp`）。本地 Windows 不区分大小写、线上 Linux 区分 —— 名字大小写对不上会**本地能开、线上 404**
2. 单张 ≤1.0 MB、总量 ≤5 MB。**先压缩，再放进 `images/`** —— 大图一旦提交进 Git 历史就永久留存，删文件也清不掉

---

## 六、想在手机上看

**路 A：现在就能做（同一 WiFi）**

1. 手机和电脑连同一个 WiFi
2. 查电脑的局域网地址：命令行里执行 `ipconfig`，找 **"IPv4 地址"**（形如 `192.168.x.x`）
3. 重新起服务，允许外部访问：

```
cd /d D:\workbuddy
python -m http.server 8000 --bind ::
```

4. 手机浏览器打开 → `http://第2步查到的那个地址:8000/`

> ⚠️ 这条命令会把 `D:\workbuddy` 暴露给同一 WiFi 下的其他人。**看完记得回命令行按 Ctrl + C 关掉**。
>
> 注：`--bind ::` 同时覆盖 IPv4 与 IPv6，手机连电脑的 `192.168.x.x` 也走这条命令，**不需要**再换成 `--bind 0.0.0.0`。

**路 B：上线之后（更省事）**

开了 GitHub Pages，直接拿公开网址在手机上开 —— 那才是真实上线形态，也是验收该走的路径。

---

## 七、上线（还没做，先占个位）

- 仓库：https://github.com/Soren-yeyey/Soren2077
- 计划做法：仓库 **Settings → Pages → Deploy from a branch**，选 `main` / `/ (root)`
- 上线后网址形如 `https://soren-yeyey.github.io/Soren2077/`

---

## 八、文件清单

| 文件 | 作用 |
|---|---|
| `index.html` | 全部内容：页面结构 + 样式 + 交互逻辑 |
| `images/home-street.webp` | 街景首页背景图 1920×1080 |
| `images/scene-laundromat.webp` | 店内场景背景图 1920×1080 |
| `images/player-neon.webp` | 角色立绘 599×1501（带透明通道），**两屏共用这一张** |
| `images/item-washer.webp` | 第 ① 幕配图：还在转的洗衣机 1440×810 |
| `images/item-vending.webp` | 第 ② 幕配图：自动贩卖机 1440×810 |
| `images/item-radio.webp` | 第 ③ 幕配图：老式收音机 1440×810 |

`images/` 里只放这六张。别往里塞截图、草稿、原图 —— 多一个文件就多一份上线体积。

---

## 九、三个验证开关（调试用）

网址后面加个参数，就能把页面直接切到非正常状态 —— 不用靠"手快"去抓加载的那一瞬间：

| 网址 | 看到什么 |
|---|---|
| `http://localhost:8000/` | **正常态**：短暂加载后出现 4 张「动静」卡片 |
| `http://localhost:8000/?mock=loading` | **加载态**：一条来回扫的霓虹线 + 「正在接入街区…」 |
| `http://localhost:8000/?mock=empty` | **空态**：「今夜街上没什么动静。」 |
| `http://localhost:8000/?mock=error` | **错误态**：「街区信号中断」+ 一个「重新连接」按钮 |

> 这几个状态是**页面自己实现的**，不是摆设：图片真的加载失败时，页面会自动进错误态并给出重连按钮，不会白屏。

---

## 十、出问题先查这里

| 现象 | 先查 |
|---|---|
| 打开是白屏 | 命令行窗口有没有报错；地址是不是 `http://localhost:8000/`（别漏 `http://`） |
| 浏览器说「无法访问此网站」「拒绝连接」 | **先看服务是不是漏了 `--bind ::`**（见第二节说明）。快速判断：把地址换成 `http://127.0.0.1:8000/`，能打开就说明就是这个原因 |
| 图片不显示 | `images/` 里文件名有没有被改过大小写；按 F12 看控制台是不是 404 |
| 底部一直转圈，或显示「街区信号中断」 | 点「重新连接」；再不行按 F12 看是不是图片 404 |
| 端口被占 | 换 8001（命令和网址都要改） |
| 点了没反应 | 按 F12 看有没有红色报错；再强刷一次 |
| 手机上打不开 | 电脑和手机是不是同一个 WiFi；服务是不是用了 `--bind ::`；电脑防火墙有没有拦 Python |
